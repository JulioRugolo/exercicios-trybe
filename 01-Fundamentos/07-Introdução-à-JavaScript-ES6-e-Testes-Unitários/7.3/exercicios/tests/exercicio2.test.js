const { info, printMessage } = require('../src/exercicio2')

describe('Testando a função printMessage', () => {
    it('Testando se o parâmetro de printMessage possui personagem', () => {
        expect(() => info[personagem] !== null).toThrow()
    })
})